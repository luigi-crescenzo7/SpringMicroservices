apiVersion: v1
kind: Service
metadata:
  name: kotlin-service
  namespace: default
  labels:
    app: kotlin-app
spec:
  selector:
    app: kotlin-app
  ports:
    -   port: 8082
        name: http
        appProtocol: http
        targetPort: 8082

